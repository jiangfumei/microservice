spring:
  profiles:
    active: dev

  application:
    name: authorization-server

  security:
    oauth2:
      jwt:
        signingKey: oauth

  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}
        file-extension: yml
